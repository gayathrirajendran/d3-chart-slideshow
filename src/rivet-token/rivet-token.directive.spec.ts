import { RivetTokenDirective } from './rivet-token.directive';

describe('RivetTokenDirective', () => {
  it('should create an instance', () => {
    const directive = new RivetTokenDirective();
    expect(directive).toBeTruthy();
  });
});
