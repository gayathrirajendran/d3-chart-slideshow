import { RivetTemplateDirective } from './rivet-template.directive';

describe('RivetTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new RivetTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
